{
  "name": "root",
  "private": true,
  "scripts": {
    "setup": "npm install && npm run bootstrap --hoist",
    "bootstrap": "lerna bootstrap",
    "lint:staged": "lerna run lint:staged --stream",
    "lint": "lerna run lint --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
